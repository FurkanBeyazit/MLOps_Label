# 자동 라벨링 파이프라인

**목적:** CCTV 영상에서 낙상자, 차량, 동물 등의 객체를 자동으로 감지하고 라벨링합니다.

---

## 1단계 — VLM: 지능형 사전 스크리닝

각 사진을 시각 언어 모델(Qwen3-VL)에 질의합니다.

> *"이 사진에 쓰러져 있거나 바닥에 누운 사람이 있습니까? 동물은 무시하세요. 서 있는 사람도 무시하세요."*

모델은 사진마다 두 가지 확률값을 반환합니다: **P(Yes)** 와 **P(No)**

| 판정 | 조건 |
|---|---|
| ✅ YES | P(Yes) ≥ 0.70 |
| ❌ NO | P(No) ≥ 0.70 |
| 🟡 REVIEW | 두 값 모두 0.70 미만 |

> 임계값 0.70은 실험을 통해 결정되었습니다. 두 확률이 모두 기준 미달인 경우(판단이 어려운 경우) 사람이 직접 확인하는 REVIEW 대기열로 이동합니다.

`[ 이미지: 터미널 로그 — YES/NO/REVIEW 흐름 ]`

`[ 이미지: REVIEW로 분류된 예시 사진 + VLM 설명 텍스트 ]`

결과는 JSON 파일로 저장됩니다. REVIEW 사진은 5단계에서 사람이 직접 검토합니다.

---

## 2단계 — Florence: 낙상자 감지

YES 판정을 받은 사진에 Florence-2 모델을 적용합니다.

프롬프트: *"a person lying on the ground"*

모델이 바닥에 누운 사람 주변에 자동으로 바운딩 박스를 그립니다.

`[ 이미지: Florence 바운딩 박스 예시 ]`

---

## 3단계 — YOLO: 기타 객체 감지

전체 사진에 YOLO11x 모델을 적용합니다.  
감지 대상: `사람, 승용차, 버스, 트럭, 자전거, 오토바이, 고양이, 개`

**중복 처리 규칙:** Florence의 낙상자 박스와 겹치는 YOLO 박스는 제거합니다 — Florence가 우선입니다.

**모델 선정:** YOLO11x, YOLO26x를 포함한 전체 모델을 비교 평가한 결과, YOLO11x가 전체 F1 기준 최고 성능을 기록했습니다.

`[ 이미지: 모델별 벤치마크 비교 표 ]`

---

## 4단계 — 병합 및 저장

Florence + YOLO 결과를 병합하여 사진별 `.txt` 라벨 파일을 생성합니다.

보유 중인 GT(Ground Truth) 라벨과 비교하여 품질을 측정합니다.

`[ 이미지: 클래스별 F1 결과 표 ]`

> 참고: 이 결과는 REVIEW 사진이 반영되기 전 수치입니다. 사람의 검토·수정이 완료되면 F1이 추가로 향상될 것으로 예상됩니다.

---

## 5단계 — Review Tool: 사람 검증

파이프라인 완료 후 시각적 검토 도구가 자동으로 실행됩니다.

### REVIEW 사진 처리

- 우측 패널에 VLM의 판단 근거가 표시됩니다  
  *(예: "이 사람은 인도 위에 쓰러져 있으며, 연석 근처에 몸이 납작하게 누워 있습니다.")*
- 라벨이 정확하면 → **Next**
- 수정이 필요하면 → 박스 그리기/삭제 → **저장**

`[ 이미지: Review Tool 화면 — VLM 설명 패널 포함 ]`

### 전체 사진 검토 (Review Gallery)

- 전체 데이터셋을 빠르게 검토할 수 있습니다
- 잘못된 라벨은 즉시 수정 가능합니다
- GT 비교 결과(TP / FP / FN)가 실시간으로 표시됩니다

`[ 이미지: Gallery 화면 — 색상별 바운딩 박스 + GT 비교 ]`

### FP/FN 시각화

오탐(FP)과 미탐(FN) 사진을 클래스별로 분류하여 저장합니다.  
어떤 상황에서 모델이 실수하는지 빠르게 파악할 수 있습니다.

`[ 이미지: FP/FN 시각화 예시 ]`

---

## 결과 요약

| 항목 | 수치 |
|---|---|
| 전체 사진 수 | 145장 |
| 자동 YES | — |
| 자동 NO | — |
| REVIEW (사람 확인 필요) | 7장 |
| 전체 F1 | ~0.7X |
| 낙상 감지 F1 | ~0.76 |

파이프라인은 사람의 개입을 최소화하면서 높은 품질의 라벨을 자동 생성합니다.  
REVIEW 비율을 낮게 유지하여 운영자 부담을 줄이고, 판단이 어려운 경우에만 사람이 최종 확인합니다.

---

## 향후 방향

### 이벤트 감지 활용

현재 파이프라인은 정오탐(TP/FP) 라벨 없이도 **쓰러짐(낙상) 이벤트** 자체를 감지하는 용도로 바로 활용할 수 있습니다.  
향후 정오탐 정보가 누적되면 해당 데이터를 바탕으로 모델을 업데이트하여 감지 정확도를 지속적으로 개선할 수 있습니다.

### 커스텀 클래스 (tiller · scooter · tractor · boar)

현재는 기존에 학습된 모델의 성능을 신뢰하여 그대로 사용합니다.  
다만 실운영 환경에서 해당 클래스의 오탐이 빈번하게 발생하거나 데이터가 충분히 확보되는 경우, 해당 클래스에 특화된 재학습을 검토할 예정입니다.

### 낙상 감지 정밀도 개선 — 진행 중인 분석

현재 파이프라인의 낙상 감지(Florence-2) 성능을 분석한 결과는 다음과 같습니다.

| 항목 | 내용 |
|---|---|
| GT falldown | 39개 (33장 직접 낙상 + 복수 낙상 포함 사진) |
| VLM YES → Florence 처리 | 32장 |
| REVIEW (사람 확인) | 7장 (이 중 TP 4, FP 3) |
| Florence 최종 결과 | TP 27, FP 5 (실제 FP: 고양이 1, 쓰레기더미 1) |

**식별된 문제점:**

1. **두 명이 나란히 쓰러진 경우** — Florence가 한 명만 감지하고 나머지를 `person`으로 분류  
2. **야간 촬영 / 불명확한 형체** — VLM이 개·비닐봉지·그림자 등을 낙상으로 오인하여 REVIEW 처리  
3. **동물 오탐** — 바닥에 누운 고양이·개를 낙상으로 인식

**개선 방향:**

- Florence 프롬프트 변형 실험 (`"a person lying on the ground, not an animal"` 등 6가지 비교)
- VLM 질의문에 명시적 조건 추가:  
  *"A dog, cat or other animal lying on the ground is NOT a fallen person."*  
  *"If the image is too dark or the shape is unclear, answer No."*
- 모델 파라미터 조정보다 **프롬프트 개선**이 더 효과적 — 145장 데이터로 파인튜닝 시 과적합 위험
